import { DisplayDollarPipe } from './display-dollar.pipe';

describe('DisplayDollarPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayDollarPipe();
    expect(pipe).toBeTruthy();
  });
});
